Helpers
	any_charactar = [0 .. 0xffff];
	letter = [['a' .. 'z'] + ['A' .. 'Z']];
	digit = ['0' .. '9'];

Tokens
	blank = (' ' | 10 | 13 | 9)*;
	identifier = (letter | '_')* + (letter | '_' | digit)*;

Ignored Tokens
	comment = ('⁄⁄' any_character* '\n') | ('/*' + any_character* + '*/')